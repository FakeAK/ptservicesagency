---
import type {Lang} from "../i18n";
import {getLocalizedPath} from "../i18n";

interface Props {
  title: string;
  date: string;
  excerpt: string;
  imageSrc: string;
  imageAlt: string;
  href: string;
  lang: Lang;
}

const {title, date, excerpt, imageSrc, imageAlt, href, lang} = Astro.props;
---

<a href={getLocalizedPath(href, lang)} class="flex flex-col gap-3 p-4 bg-white rounded-xl border border-border">
  <img src={imageSrc} alt={imageAlt} class="w-full aspect-16/9 rounded-lg object-cover" loading="lazy" />
  <div class="flex flex-col">
    <h3 class="text-left text-2xl font-bold leading-tight text-primary">
      {title}
    </h3>
    <p class="mb-0 mt-1 text-sm text-body-text/60">{date}</p>
    <p class="mb-0 mt-1 text-[15px] text-body-text">{excerpt}</p>
  </div>
</a>
